apiVersion: 1

providers:
  - name: 'OIB Telemetry Dashboards'
    orgId: 1
    folder: 'OIB'
    folderUid: 'oib'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
